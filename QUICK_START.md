# 🚀 Quick Start - Zyfra 1.0

## Минимальная инструкция для запуска

### 1. Убедитесь, что Docker установлен

```bash
docker --version
docker-compose --version
```

Если не установлен - скачайте [Docker Desktop](https://www.docker.com/products/docker-desktop/)

---

### 2. Запустите приложение

#### С помощью Docker Compose (рекомендуется):

```bash
docker-compose up -d
```

#### С помощью Makefile (если установлен make):

```bash
make up
```

---

### 3. Откройте в браузере

```
http://localhost:4200
```

---

## 🎯 Основные команды

| Команда | Описание |
|---------|----------|
| `docker-compose up -d` | Запустить приложение |
| `docker-compose down` | Остановить приложение |
| `docker-compose logs -f` | Посмотреть логи |
| `docker-compose restart` | Перезапустить |
| `docker-compose build --no-cache` | Пересобрать без кэша |

---

## 💡 С Makefile еще проще

```bash
make up        # Запустить
make down      # Остановить
make logs      # Логи
make restart   # Перезапустить
make rebuild   # Пересобрать
make help      # Все команды
```

---

## 🐛 Проблемы?

### Порт 4200 занят?

Измените в `docker-compose.yml`:
```yaml
ports:
  - "8080:80"  # Измените 4200 на любой свободный порт
```

### Ошибка при сборке?

```bash
docker-compose build --no-cache
docker-compose up -d
```

### Посмотреть что происходит?

```bash
docker-compose logs -f
```

---

## 📚 Подробная документация

Смотрите [DOCKER_GUIDE.md](DOCKER_GUIDE.md) для полной документации.

---

**Готово!** 🎉 Ваше приложение должно работать на http://localhost:4200

