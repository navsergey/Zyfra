# 🐳 Docker Guide - Zyfra 1.0

## 📦 Созданные файлы

1. **Dockerfile** - многоступенчатая сборка приложения (multi-stage build)
2. **.dockerignore** - исключение ненужных файлов из Docker образа
3. **nginx.conf** - оптимизированная конфигурация Nginx для Angular SPA
4. **docker-compose.yml** - упрощенный запуск через Docker Compose

---

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

```bash
# Сборка и запуск в фоновом режиме
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка приложения
docker-compose down

# Остановка и удаление volumes
docker-compose down -v
```

**Приложение будет доступно по адресу:** http://localhost:4200

### Вариант 2: Docker напрямую

```bash
# Сборка образа
docker build -t zyfra-app:latest .

# Запуск контейнера
docker run -d \
  -p 4200:80 \
  --name zyfra-container \
  --restart unless-stopped \
  zyfra-app:latest

# Просмотр логов
docker logs -f zyfra-container

# Остановка контейнера
docker stop zyfra-container

# Удаление контейнера
docker rm zyfra-container
```

---

## 📋 Структура Dockerfile

### Stage 1: Build (Node.js)
- **Образ:** `node:20-alpine` (легковесный)
- **Действия:**
  - Копирование зависимостей
  - Установка через `npm ci --legacy-peer-deps`
  - Production сборка Angular приложения

### Stage 2: Production (Nginx)
- **Образ:** `nginx:alpine` (минимальный размер)
- **Действия:**
  - Копирование собранного приложения
  - Настройка Nginx для SPA routing
  - Экспорт порта 80

**Размер финального образа:** ~50-60 МБ 🎯

---

## ⚙️ Конфигурация Nginx

### Основные возможности:
- ✅ **SPA Routing** - все маршруты обрабатываются через `index.html`
- ✅ **Gzip сжатие** - оптимизация размера передаваемых файлов
- ✅ **Кэширование** - статические файлы кэшируются на 1 год
- ✅ **Безопасность** - защита от XSS, clickjacking
- ✅ **Обработка ошибок** - перенаправление 404/500 на index.html

---

## 🛠️ Полезные команды

### Управление контейнерами

```bash
# Список всех контейнеров
docker ps -a

# Перезапуск контейнера
docker restart zyfra-container

# Вход в контейнер (для отладки)
docker exec -it zyfra-container sh

# Просмотр использования ресурсов
docker stats zyfra-container
```

### Управление образами

```bash
# Список всех образов
docker images

# Удаление образа
docker rmi zyfra-app:latest

# Пересборка без кэша
docker build --no-cache -t zyfra-app:latest .
```

### Очистка

```bash
# Удаление неиспользуемых контейнеров
docker container prune

# Удаление неиспользуемых образов
docker image prune -a

# Полная очистка Docker (осторожно!)
docker system prune -a --volumes
```

---

## 🔧 Настройка

### Изменение порта

Если порт 4200 занят, измените в `docker-compose.yml`:

```yaml
ports:
  - "8080:80"  # Приложение будет на http://localhost:8080
```

Или при запуске напрямую:

```bash
docker run -d -p 8080:80 --name zyfra-container zyfra-app:latest
```

### Переменные окружения

Добавьте в `docker-compose.yml`:

```yaml
environment:
  - NODE_ENV=production
  - API_URL=https://api.example.com
```

---

## 🌐 Production Deployment

### Рекомендации для продакшена:

1. **Reverse Proxy** - используйте Traefik, Nginx Proxy Manager или Caddy
2. **HTTPS** - настройте SSL сертификаты (Let's Encrypt)
3. **Мониторинг** - добавьте Prometheus + Grafana
4. **Логирование** - настройте централизованное логирование
5. **Backup** - регулярное резервное копирование данных

### Пример с Traefik:

```yaml
version: '3.8'

services:
  support-rag-frontend:
    build: .
    container_name: zyfra-app
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zyfra.rule=Host(`app.example.com`)"
      - "traefik.http.routers.zyfra.entrypoints=websecure"
      - "traefik.http.routers.zyfra.tls.certresolver=letsencrypt"
    networks:
      - web

networks:
  web:
    external: true
```

---

## 🐛 Troubleshooting

### Проблема: "port already in use"

**Решение:**
```bash
# Найти процесс на порту 4200
netstat -ano | findstr :4200  # Windows
lsof -i :4200                  # Linux/Mac

# Остановить процесс или изменить порт
```

### Проблема: Ошибка при сборке

**Решение:**
```bash
# Очистить кэш и пересобрать
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: Приложение не загружается

**Решение:**
```bash
# Проверить логи
docker-compose logs -f

# Проверить статус контейнера
docker ps -a

# Проверить конфигурацию Nginx внутри контейнера
docker exec -it zyfra-app cat /etc/nginx/conf.d/default.conf
```

### Проблема: 502 Bad Gateway

**Решение:**
- Проверьте, что приложение собралось корректно
- Проверьте путь к собранным файлам в Dockerfile
- Убедитесь, что порт 80 не занят в контейнере

---

## 📊 Требования

- **Docker:** версия 20.10 или выше
- **Docker Compose:** версия 2.0 или выше
- **RAM:** минимум 2 GB для сборки
- **Disk:** минимум 500 MB свободного места

---

## 📈 Оптимизация

### Уменьшение размера образа:
- ✅ Multi-stage build (уже реализовано)
- ✅ Alpine Linux (минимальный базовый образ)
- ✅ .dockerignore (исключение ненужных файлов)

### Ускорение сборки:
- Используйте кэширование слоев Docker
- Разделяйте изменяемые и неизменяемые части

---

## 📝 Логи

### Просмотр логов:

```bash
# Все логи
docker-compose logs

# Последние 100 строк
docker-compose logs --tail=100

# Следить за логами в реальном времени
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f support-rag-frontend
```

---

## 🔒 Безопасность

Конфигурация Nginx включает следующие заголовки безопасности:

- `X-Frame-Options: SAMEORIGIN` - защита от clickjacking
- `X-Content-Type-Options: nosniff` - защита от MIME sniffing
- `X-XSS-Protection: 1; mode=block` - защита от XSS
- `Referrer-Policy` - контроль передачи referrer

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Проверьте статус: `docker-compose ps`
3. Пересоберите без кэша: `docker-compose build --no-cache`

---

**Готово к использованию!** 🎉

Просто выполните `docker-compose up -d` и ваше приложение запустится!

